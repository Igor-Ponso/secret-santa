# Procfile
# -----------------------------------------------------------------------------
# Defines the web process for the platform buildpack runtime.
# We delegate all startup responsibilities (migrate, caches, launch server)
# to start.sh so the definition here stays minimal.
# -----------------------------------------------------------------------------
web: bash start.sh