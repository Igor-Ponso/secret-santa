// Temporary module declarations to satisfy TypeScript until vue-i18n types resolve correctly.
// vue-i18n ships its own types, but if tsconfig path/resolution causes issues we declare a module fallback.
declare module 'vue-i18n';
